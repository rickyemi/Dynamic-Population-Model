# Development and CI dependencies. Install both files:
#
#     pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
ruff>=0.6            # linting and formatting
nbformat>=5.10       # tools/nb_to_module.py reads the notebooks with it
pre-commit>=3.7
notebook>=7.0        # to run the notebooks in notebooks/
jupyterlab>=4.0
