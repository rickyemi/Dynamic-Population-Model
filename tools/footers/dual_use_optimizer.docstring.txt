Dual use calculation optimizer.

Generated from ``notebooks/03_dual_use_calculation_optimizer.ipynb`` by ``tools/nb_to_module.py``.
Edit the notebook, then regenerate; do not edit this file by hand.

Rebuilds the ``Difference`` tab of a Dual Use Calculations workbook from its two source tabs and
writes a new workbook carrying both source tabs plus the rebuilt tab as ``Difference_calc``.

Input requirements:

1. File name ``Dual_Use_Calculations_TwoAge_B01_<Product>_<Month>_<Day>_<Year>.xlsx``. The product
   and the run date are read from it.
2. A ``Detailed Results`` tab and a ``MasterModels TP-ID`` tab. Names are matched on letters and
   digits only, so ``Raw Detailed Results`` and ``MasterModels TP-1D`` both qualify: the digit one
   and the letter I are indistinguishable in most fonts.
3. ``Model Group`` reading ``<Product>_<analyst initials>``, checked against the file name.

Every value is written twice: as a live Excel formula so the sheet recalculates as the hand built
version did, and as a Python number so ``validate_against_source`` can compare the rebuild against
the original tab cell by cell.

Public API:  ``run_pipeline()``, ``validate_against_source()``, ``validate_workbook()``,
``self_test()``.  Command line access is through ``dpm.cli`` (``dpm dual-use ...``).
